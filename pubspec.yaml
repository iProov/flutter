name: iproov_sdk
description: Flexible authentication for identity assurance
homepage: https://www.iproov.com/
version: 2021.01.01
author: iProov

environment:
  sdk: ">=2.7.0 <3.0.0"
  flutter: ">=1.20.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  plugin:
    platforms:
      android:
        package: com.iproov.sdk
        pluginClass: IProovSDKPlugin
      ios:
        pluginClass: IProovSDKPlugin
