name: iproov_flutter
description: The iProov Biometrics SDK for Flutter
version: 0.1.0
author:
homepage: https://github.com/iProov/flutter

environment:
  sdk: ">=2.7.0 <3.0.0"
  flutter: ">=1.20.0"

dependencies:
  flutter:
    sdk: flutter
  image: ^3.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  plugin:
    platforms:
      android:
        package: com.iproov.sdk
        pluginClass: IProovSDKPlugin

      ios:
        pluginClass: IProovSDKPlugin
